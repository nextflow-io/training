/*
 * Local execution configuration
 */

process {
    executor = 'local'

    // Default resource requirements for local execution
    cpus = 1
    memory = '1.GB'
}

// Local executor resource limits
executor {
    cpus = 4
    memory = '8.GB'
}
