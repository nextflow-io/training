/*
 * Base configuration - settings common to all environments
 */

process {
    // Default resource requirements
    cpus = 1
    memory = '1.GB'
    time = '1.h'

    // Error handling
    errorStrategy = 'retry'
    maxRetries = 2
}
