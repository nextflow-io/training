[build]

# Directory that contains the deploy-ready HTML files and
# assets generated by the build. This is relative to the base
# directory if one has been set, or the root directory if
# a base has not been set.

publish = "build/"

# Default build command.
command = """

    # training
    # For social cards - not sure if these are really needed or not
    apt-get install libcairo2-dev libfreetype6-dev libffi-dev libjpeg-dev libpng-dev libz-dev
    mkdocs build --site-dir build

    # asciidoctor install
    curl -s "https://get.sdkman.io" | bash
    source "/opt/buildhome/.sdkman/bin/sdkman-init.sh"
    sdk install asciidoctorj

    # hands-on
    mkdir -p build/hands-on
    pushd handson-docs
    asciidoctorj -D ../build/hands-on index.adoc
    cp -r img ../build/hands-on/img
    popd
    """
