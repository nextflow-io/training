nextflow_process {

    name "Test Process SAMTOOLS_INDEX"
    script "../main.nf"
    process "SAMTOOLS_INDEX"

    test("Should index reads_mother.bam correctly") {

        when {
            params {
                outdir = "tests/results"
            }
            process {
                """
                input[0] = file("${projectDir}/data/bam/reads_mother.bam")
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

    test("Should index reads_father.bam correctly") {

        when {
            params {
                outdir = "tests/results"
            }
            process {
                """
                input[0] = file("${projectDir}/data/bam/reads_father.bam")
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

    test("Should index reads_son.bam correctly") {

        when {
            params {
                // define parameters here. Example:
                // params.reads_bam = "${projectDir}/data/sample_bams_1.txt"
                outdir = "tests/results"
                // params.outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = Channel.fromPath(params.reads_bam).splitText() // reads_ch = Channel.fromPath(params.reads_bam).splitText()
                input[0] = file("${projectDir}/data/bam/reads_son.bam")
                """
            }
        }

        then {
            assert process.success  // expect the process to run without errors
            assert snapshot(process.out).match()   //expect result of run to be identical to result from a previous run
        }

    }

}
